#ifndef DELTA_COEFF_H_INCLUDED
#define DELTA_COEFF_H_INCLUDED
	//double** delta_coeff_mat(double**, int, int, int);
	double * delta_coeff_arr(double *, int, int);
	//double** delta_2coeff_mat(double **, int, int, int);
	double * delta_2coeff_arr(double *, int, int);
#endif
